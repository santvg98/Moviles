import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.senSant.myProyect',
  appName: 'myProyect',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
